export default function MapingArrey() {
  const fruits = ['apple', 'banana', 'orange'];
  const cake = fruits.map((anything) => {
    return anything + 'Cake';
  });
  return;
}

/* .map ruft einfe funktion auf, welche alles über die arrey loopt*/
